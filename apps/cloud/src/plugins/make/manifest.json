{
  "id": "make",
  "name": "Make",
  "description": "Visual workflow automation with Make (formerly Integromat) — trigger scenarios, manage organizations, and connect 1,500+ apps",
  "version": "1.0.0",
  "category": "automation",
  "icon": "workflow",
  "website": "https://www.make.com",
  "docs": "https://www.make.com/en/api-documentation",
  "auth": {
    "type": "api-key",
    "fields": [
      {
        "key": "MAKE_API_TOKEN",
        "label": "API Token",
        "description": "Make API token from your profile settings",
        "required": true,
        "sensitive": true
      },
      {
        "key": "MAKE_ZONE",
        "label": "API Zone",
        "description": "Make API zone (e.g., eu1, eu2, us1)",
        "required": false,
        "sensitive": false,
        "placeholder": "eu1"
      }
    ]
  },
  "config": {
    "type": "object",
    "properties": {
      "organizationId": {
        "type": "string",
        "description": "Make organization ID to scope API calls"
      },
      "teamId": {
        "type": "string",
        "description": "Make team ID for team-scoped operations"
      }
    }
  },
  "capabilities": ["webhook", "action"],
  "tags": ["automation", "workflow", "integration", "no-code"],
  "popularity": 72
}
