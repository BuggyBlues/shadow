{
  "id": "neon",
  "name": "Neon",
  "description": "Serverless Postgres with branching, migrations, and SQL execution via the Neon MCP server",
  "version": "1.0.0",
  "category": "database",
  "icon": "database",
  "website": "https://neon.tech",
  "docs": "https://neon.tech/docs/ai/neon-mcp-server",
  "auth": {
    "type": "api-key",
    "fields": [
      {
        "key": "NEON_API_KEY",
        "label": "Neon API Key",
        "description": "API key from Neon Console (Settings → API keys). Supports project management, branching, and SQL execution.",
        "required": true,
        "sensitive": true,
        "placeholder": "neon_..."
      }
    ]
  },
  "config": {
    "type": "object",
    "properties": {
      "readOnly": {
        "type": "boolean",
        "default": false,
        "description": "Restrict to read-only operations (list projects, describe schemas, query data)"
      }
    }
  },
  "capabilities": ["tool", "data-source"],
  "tags": ["database", "postgres", "serverless", "branching", "migrations", "mcp"],
  "popularity": 78
}
