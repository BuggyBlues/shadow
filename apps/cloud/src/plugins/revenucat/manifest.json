{
  "id": "revenucat",
  "name": "RevenueCat",
  "description": "Connect agents to RevenueCat for in-app subscription management including offerings, products, entitlements, packages, and price experiments via MCP server",
  "version": "1.0.0",
  "category": "finance",
  "icon": "dollar-sign",
  "website": "https://revenuecat.com",
  "docs": "https://docs.revenuecat.com/reference",
  "auth": {
    "type": "api-key",
    "fields": [
      {
        "key": "REVENUECAT_SECRET_KEY",
        "label": "RevenueCat Secret API Key",
        "description": "Secret API key from your RevenueCat project settings (v2 REST API)",
        "required": true,
        "sensitive": true,
        "placeholder": "sk_..."
      }
    ]
  },
  "config": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "RevenueCat project ID from your dashboard URL or project settings"
      }
    }
  },
  "capabilities": ["tool", "data-source", "action"],
  "tags": ["finance", "subscriptions", "mobile", "in-app-purchases", "mcp"],
  "popularity": 58
}
