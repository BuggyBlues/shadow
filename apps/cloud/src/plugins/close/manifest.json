{
  "id": "close",
  "name": "Close",
  "description": "Sales CRM with leads, contacts, activities, sequences, and pipeline management via Close REST API",
  "version": "1.0.0",
  "category": "crm",
  "icon": "phone",
  "website": "https://close.com",
  "docs": "https://developer.close.com/api",
  "auth": {
    "type": "api-key",
    "fields": [
      {
        "key": "CLOSE_API_KEY",
        "label": "Close API Key",
        "description": "API key from Settings > Developer > API Keys",
        "required": true,
        "sensitive": true,
        "placeholder": "api_..."
      }
    ]
  },
  "config": {
    "type": "object",
    "properties": {
      "scope": {
        "type": "string",
        "enum": ["read", "write_safe", "write_destructive"],
        "default": "read",
        "description": "API access scope: read (read-only), write_safe (read + create/update), write_destructive (full access including deletes)"
      }
    }
  },
  "capabilities": ["tool", "data-source", "action"],
  "tags": ["crm", "sales", "leads", "pipeline"],
  "popularity": 55
}
